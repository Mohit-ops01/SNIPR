version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:14
    container_name: SNIPR-db
    environment:
      POSTGRES_DB: SNIPR_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - SNIPR-network

  # Spring Boot Application
  app:
    build: .
    container_name: SNIPR-app
    depends_on:
      - postgres
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/SNIPR_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
      JWT_SECRET: your_production_jwt_secret_here
      JWT_EXPIRATION: 86400000
    ports:
      - "8080:8080"
    networks:
      - SNIPR-network

volumes:
  postgres_data:

networks:
  SNIPR-network:
    driver: bridge
